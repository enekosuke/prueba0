# Esquema GraphQL generado automáticamente.

type ProductVariantModel {
  id: ID!
  name: String!
  sku: String!
  stock: Int!
  price: Float
}

type ProductModel {
  id: ID!
  slug: String!
  title: String!
  description: String!
  images: [String!]!
  variants: [ProductVariantModel!]!
  rating: Float
}

type CategoryModel {
  id: ID!
  name: String!
  slug: String!
}

type Query {
  products(search: String, categorySlug: String, take: Int, skip: Int): [ProductModel!]!
  product(slug: String!): ProductModel
  categories: [CategoryModel!]!
}
